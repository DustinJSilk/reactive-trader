const config = require('../config');

class Backtester {
  constructor() {

  }

  run() {
    // TODO: Run backtest with current config
    // gekkomanager -> runserver
    // Hit the api/backtest api to get json response.
  }
}

module.exports = Backtester;
